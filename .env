# Environment configuration for Experimental Robotics Docker Environment
# You can customize these values as needed

# ROS Domain ID (ensures all containers can communicate)
# Default: 43 (change if running multiple ROS2 systems on same network)
ROS_DOMAIN_ID=43

# Display settings for GUI applications (usually auto-detected)
# DISPLAY=:0

# Robot Configuration
# Set MECANUM=True for mecanum wheel drive, False for differential drive
# ROSbot 2R: typically False, ROSbot XL: typically True
MECANUM=False

# Gazebo configuration
GAZEBO_MODEL_PATH=./models
GAZEBO_RESOURCE_PATH=./worlds

# Development settings
WORKSPACE_ROOT=./
LOG_LEVEL=INFO

# Optional: Set a specific RMW implementation (Fast DDS by default)
ROS_RMW_IMPLEMENTATION=rmw_fastrtps_cpp

# Real robot bridge (ROS1 <-> ROS2)
# Set these when connecting to a physical ROSbot running ROS1/Noetic.
#   ROS1_MASTER_URI = http://<rosbot-ip>:11311
#   ROS1_LOCAL_IP   = IP of your laptop on the robot's network
#   ROS1_BRIDGE_HOST_PATH = Where ./scripts/setup_ros1_bridge.sh stores the ros-humble-ros1-bridge assets
#   ROS1_BRIDGE_TOPICS_FILE = Path to the parameter_bridge topics YAML (host path)
ROS1_MASTER_URI=http://10.186.13.23:11311
ROS1_LOCAL_IP=10.186.13.2
ROS1_BRIDGE_HOST_PATH=./ros-humble-ros1-bridge
ROS1_BRIDGE_TOPICS_FILE=./config/ros1_bridge/topics.yaml

# Optional: Custom network settings (usually not needed)
# COMPOSE_PROJECT_NAME=experimental_robotics
